import { SandboxRpcClient } from "./transport.js";
/** Environment variable management namespace. */
export interface SandboxEnv {
    /** Gets the value of an environment variable.
     *
     * ```ts
     * import { Sandbox } from "@deno/sandbox";
     *
     * await using sandbox = await Sandbox.create();
     * const value = await sandbox.env.get("PATH");
     * console.log(value);
     * ```
     */
    get(key: string): Promise<string | undefined>;
    /** Sets the value of an environment variable.
     *
     * ```ts
     * import { Sandbox } from "@deno/sandbox";
     *
     * await using sandbox = await Sandbox.create();
     * await sandbox.env.set("MY_VAR", "value");
     * ```
     */
    set(key: string, value: string): Promise<void>;
    /** Gets all environment variables as an object.
     *
     * ```ts
     * import { Sandbox } from "@deno/sandbox";
     *
     * await using sandbox = await Sandbox.create();
     * const envVars = await sandbox.env.toObject();
     * console.log(envVars);
     * ```
     */
    toObject(): Promise<Record<string, string>>;
    /** Deletes an environment variable.
     *
     * ```ts
     * import { Sandbox } from "@deno/sandbox";
     *
     * await using sandbox = await Sandbox.create();
     * await sandbox.env.delete("MY_VAR");
     * ```
     */
    delete(key: string): Promise<void>;
}
export declare class SandboxEnvImpl implements SandboxEnv {
    #private;
    constructor(client: SandboxRpcClient);
    get(key: string): Promise<string | undefined>;
    set(key: string, value: string): Promise<void>;
    toObject(): Promise<Record<string, string>>;
    delete(key: string): Promise<void>;
}
//# sourceMappingURL=env.d.ts.map